<script type='text/javascript'><!--

	tt_cancel_show='/'
	tt_cancel_destination='/'

//-->
</script>

<%= form_tag :action => 'create', :method => :post  do %>

 <%= select_tag "stock[supplier_id]", options_for_select(@supplier_map),
    {:id => "supplier_id",:field_type => 'alpha',
    :helpText => "Select Items Provider"} %>

  <%= text_field_tag "stock[grn_number]", nil,
       {:id => 'grn_number', :field_type => 'alpha',
        :helpText => 'Enter GRN/Batch Number'} %>

  <%= text_field_tag "stock[grn_date]", nil,
       {:id => 'grn_date', :field_type => 'date',
        :helpText => 'Enter Date of Delivery'} %>

  <%= submit_tag "Create Stock" %>


<% end %>